# Example environment variables (safe to commit). NEVER put real API keys here.
# Copy this file to `.env` and replace placeholders with your real values.
# Make sure `.env` and `.env.*` are listed in .gitignore (they are by default).

# Core
EMBEDDINGS_BACKEND=sbert
LLM_BACKEND=openrouter

# OpenRouter
# Replace the API key placeholder in your local `.env` (do not commit):
OPENROUTER_API_KEY=<YOUR_OPENROUTER_API_KEY>
OPENROUTER_BASE_URL=https://openrouter.ai/api
OPENROUTER_CHAT_MODEL=google/gemini-2.0-flash-exp:free
OPENROUTER_EMBED_MODEL=thenlper/gte-large
OPENROUTER_SITE_URL=https://yourapp.example.com
OPENROUTER_APP_NAME=CourseTutor-MVP

# Sbert
SBERT_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Ollama
OLLAMA_MODEL=llama3
OLLAMA_EMBED_MODEL=nomic-embed-text

# Other
TOP_K=5
CHUNK_TOKENS=800
CHUNK_OVERLAP=80

# Text-to-Speech (optional)
# Set to 'pyttsx3' to enable offline TTS playback in the UI, or 'off' to disable
TTS_BACKEND=off